cmake_minimum_required(VERSION 3.2)

PROJECT(array_list)

INCLUDE_DIRECTORIES(
include
)

AUX_SOURCE_DIRECTORY(src DIR_SRCS)

SET(ARRAY_LIST
${DIR_SRCS}
)

ADD_EXECUTABLE(${PROJECT_NAME} ${ARRAY_LIST})

TARGET_LINK_LIBRARIES(${PROJECT_NAME} m)