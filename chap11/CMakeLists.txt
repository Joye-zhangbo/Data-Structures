cmake_minimum_required(VERSION 3.2)

PROJECT(binary_tree)

INCLUDE_DIRECTORIES(
include
)

AUX_SOURCE_DIRECTORY(src DIR_SRCS)

SET(BINARY_TREE
${DIR_SRCS}
)

ADD_EXECUTABLE(${PROJECT_NAME} ${BINARY_TREE})

TARGET_LINK_LIBRARIES(${PROJECT_NAME} m)
